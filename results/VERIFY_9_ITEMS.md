# ADVERSARIAL VERIFICATION OF THE 9 WORKER ITEMS  (run wf_19435609-532, 18 agents)

Every item below is DEFAULT-OFF. gate_g4_cpu.py still prints dG=-0.0030 width=1092,
so NONE of these defects touch the 48 factorial runs in flight.

NOT-READY (must not be enabled): mordred, u5u6, w7edge, monitor
READY-WITH-FIXES: w6wire, u3u4, w9, e2e, catalogue


## w6wire  -- ready-with-fixes (fatal=0)

### [major] PEMGraphTransformer silently ignores the descriptor block, and the worker left the guard out claiming they 'could not confirm which flag selects that model'. I confirmed it from the source they already had open: hydro_net.py:824 `model_arch = model_arch or CFG.model_arch`, hydro_net.py:841 `if model_arch == "graph_transformer":`, default at repo/model/model_cfg.py:65 `model_arch = "pem"`. I verified the hazard is real: PEMGraphTransformer.forward slices flat_x[:, :16], [16:48], [-20:], and [-1044:-20]. With a K-wide descriptor block inserted at 48, EVERY one of those slices remains in-bounds and returns a correctly-shaped tensor. Nothing raises. The run completes, the descriptor block is never read, and the result is reported as a descriptor arm. This is precisely the silent-wrong-number failure mode.

**FIX:** Add to PEMGraphTransformer.__init__ (after `self.llm_index = -1044`), NOT to forward — failing at construction is cheaper than failing after data loading:

        # W6: this model reassembles nodes from left-anchored slices as
        # [dist, bonded, emb, one_hot] and would SILENTLY IGNORE a descriptor block.
        if (getattr(CFG, 'aa_descriptors', 'none') or 'none') != 'none':
            raise ValueError(
                "W6: --aa_descriptors is implemented for model_arch='pem' only. "
                "PEMGraphTransformer slices nodes left-anchored and would ignore the "
                "descriptor block silently, reporting a baseline result as a descriptor "
                "result. Use --aa_descriptors none with model_arch='graph_transformer'.")

Then add a gate check asserting that combination raises.

**BLOCKER:** Not blocking my item, but MUST be handled before a graph-transformer arm: PEMGraphTransformer is not wired for descriptors and would silently ignore the block rather than erroring. That combination must be BLOCKED, not assumed to work. A guard at the top of its forward is the right fix; I left it out because I could not confirm from the source which flag selects that model.

**BLOCKER:** pca16 / pca16_only cannot run until data/aa_descriptors_pca16.csv is built and committed. gate_w6.py SKIPS those arms loudly and exits NON-ZERO in that case - a skipped check is not a passed check. curated12 can run today against the existing data/aa_descriptors.csv.

**BLOCKER:** No cluster access and no torch on this machine, so G0-G4 have not been executed against the real CSV or a real PEM. The manager must run `python scripts/gate_w6.py` plus `python scripts/gate_g4_cpu.py` (W5/U2 must still pass) before any W6 training run.

## mordred  -- not-ready (fatal=3)

### [fatal] FALSE PREMISE: "The consumer code does not exist yet." It does exist, in the ground-truth branch, and is fully wired. srclive/aa_descriptors.py is imported at srclive/train_utils.py line 12 (aa_descriptor_mode, desc_or_none, descriptor_dim, keeps_one_hot), used at train_utils.py lines 280-282, 292, 321, 587, 645; hydro_net.py lines 298-315 and 371-374 size fc1_gcn/fc1_gat from it; train.py lines 85-86 already expose --aa_descriptors {none,pca16,curated12,pca16_only} and --aa_descriptor_csv. The worker declared a contract for a consumer that was already written, and never read it. Consequence: the concrete filename and column-count mismatches below, both of which the worker would have caught.

**FIX:** Read srclive/aa_descriptors.py before finalising. Delete the "consumer does not exist" claim from REPORT.md section 9 item 2 and PATCH.md, and replace it with the verified fact that aa_descriptors.py already reorders rows by AA_MAP explicitly (load_descriptor_table: `table = torch.tensor([rows[a] for a in ORDER])`) and validates row labels. The positional row-order contract is therefore already satisfied by label, not position — gate check 1 is still useful but is belt-and-braces, not the binding contract.

### [fatal] FILENAME MISMATCH — the pipeline's output can never be loaded. build_aa_descriptors_mordred.py writes `data/aa_descriptors_mordred.csv` (--out default, line 251) and derives the PCA arm as `data/aa_descriptors_mordred_pca16.csv` (line 463). INSTALL.md lines 102/132/135-136 instruct exactly those names. But srclive/aa_descriptors.py _DEFAULT_FILES (lines 41-45) loads 'pca16' and 'pca16_only' from `data/aa_descriptors_pca16.csv` and 'curated12' from `data/aa_descriptors.csv`. Neither produced file matches. Running the documented sequence then launching `--aa_descriptors pca16` raises FileNotFoundError from _resolve(); worse, if a stale `data/aa_descriptors_pca16.csv` from the OLD curated pipeline exists, training silently loads the OLD matrix and the entire Mordred item is a no-op that reports as a descriptor result.

**FIX:** Either (a) write to the name the consumer already reads — change --out default to `data/aa_descriptors.csv` so the PCA arm derives `data/aa_descriptors_pca16.csv`, and update INSTALL.md lines 102/132/135-136 to match; or (b) keep the _mordred names and pass them explicitly at train time via the existing override, `--aa_descriptor_csv data/aa_descriptors_mordred_pca16.csv`. Option (a) is preferable — it needs no per-run flag and cannot be forgotten. Whichever is chosen, state in INSTALL.md that any pre-existing data/aa_descriptors_pca16.csv must be deleted or archived first, so a stale curated matrix cannot be silently picked up.

### [fatal] THE PCA UNIT-VARIANCE RESCALE DESTROYS THE CHEMISTRY AND FAILS THE ITEM'S OWN GATE. build_aa_descriptors_mordred.py lines 443-446 divide each principal component by its own std (`zsd = Z.std(axis=0, ddof=0); Z / zsd`). That is a whitening transform, not a projection: it is not distance-preserving. I verified this numerically using the repo's own fixture. Without the rescale the PCA is a pure rotation (max |dist_original - dist_PCA| = 8.4e-08) and the gate exits 0 at every k tested (4, 6, 8, 10). With the shipped rescale the geometry is destroyed (max distance change 3.53) and the gate exits 1 at k=8 and k=10, with BLOCKING failures on D (neighbours E,Q,Y not within {E,N}) and F (neighbours Y,V,A not within {W,Y}). Per-component stds fall away fast (2.13, 1.69, 1.22, 0.90, 0.41, 0.28, 0.22, 0.13, 0.11, 0.06), so dividing by them inflates trailing near-noise components to equal weight with the chemistry-carrying leading ones. The shipped default is --pca 16, i.e. squarely in the failing regime. The worker never ran this because rdkit is absent, so it is reported as untested.

**FIX:** Delete the rescale at build_aa_descriptors_mordred.py lines 443-446 and write `pca_df = pd.DataFrame(Z, index=ORDER, columns=[...])`, keeping the natural PC scaling so the projection stays distance-preserving and the retained-variance ordering is meaningful. Remove the now-false 'pca_components_rescaled' provenance line. If per-column scale genuinely matters downstream, note that it does not here: the model applies a Linear directly to this block, which learns its own per-column scale, so whitening buys nothing and costs the geometry. Then re-run the gate on the pca arm and require exit 0 before use.

### [major] SILENT-NO-OP RISK ACROSS THE ARMS is not surfaced anywhere. srclive/aa_descriptors.py maps 'pca16' and 'pca16_only' to the SAME file and 'curated12' to `data/aa_descriptors.csv`, and descriptor_dim() reads K from whatever CSV is actually on disk rather than hard-coding 16/12. So a Mordred matrix dropped at the curated12 path would silently change K and resize fc1_gcn/fc1_gat with no error. Combined with the filename mismatch above, there is no check anywhere that the file being trained on is the file this item produced.

**FIX:** Have verify_descriptors_mordred.py additionally assert the provenance header contains `generator=build_aa_descriptors_mordred.py`, and add a line to INSTALL.md step 2 instructing the operator to run the gate against the exact path the training run will load (`data/aa_descriptors_pca16.csv` after the rename), not merely against the file just written. This closes the stale-file hole.

**BLOCKER:** The Mordred pipeline has NOT been run anywhere — rdkit and mordred are absent from this machine and from the cluster by design. The scripts are authored and self-tested (SMILES selftest 20/20, gate self-check 5/5) but data/aa_descriptors_mordred.csv does not yet exist. Manager must create mordred_env per INSTALL.md, run the pipeline with --pca 16, and run the gate.

**BLOCKER:** Therefore the real filtered column counts and the PCA variance-explained figure are unknown until the pipeline runs. They are emitted into the CSV header and stdout automatically, but no number can be quoted yet.

**BLOCKER:** The actual rdkit/mordred versions that will be used could not be determined (no cluster access, neither package installed anywhere reachable). INSTALL.md pins versions with reasons; the script stamps whatever actually ran into the provenance header.

**BLOCKER:** Could not inspect whether esm2_env_py38 has numpy<1.24. Deliberately not needed — nothing written here installs into it, and the gate needs only numpy+pandas at any version.

**BLOCKER:** Unresolved inconsistency in the pre-existing scratchpad/build_aa_descriptors.py (NOT edited by me): its CURATED rows carry 14 values and CURATED_COLS names 14 columns, but its own docstring and PHASE 6.2 both say 12. Whoever owns the curated12 arm should reconcile this before it is reported.

**BLOCKER:** The doc correction in PATCH.md (histidine SMILES in PHASES_5_9_WITH_CODE.md line 230) has NOT been applied — I do not edit planning docs. Until applied, the next worker copying that table gets the wrong molecule.

## u5u6  -- not-ready (fatal=2)

### [fatal] coil_bond_length (coil_topology.py:211) duplicates the PRE-U4 arithmetic and hardcodes the 'fitted' branch. The worker's stated assumption -- 'coil_bond_length reproduces _flory_unfolded_graph's b EXACTLY, defect A4 included ... if the A4 arm lands, both must change TOGETHER' -- is FALSE against the live source: the A4 arm HAS ALREADY LANDED. srclive/train_utils.py:620 calls _coil_bond_length(ca, N, dev, x.dtype), defined at train_utils.py:481, which branches on CFG.coil_b ('fitted'|'fixed'), and --coil_b is a live argparse flag (train.py:80) bound at train.py:106. Under `--flory_unfolded --coil_b fixed --coil_edges`, the NODE-feature coil uses b = _COIL_B_FIXED = 5.82*0.1 = 0.582 while the U6 EDGE coil uses the fitted b (~0.38 on the 10x-rescaled coords). Edge and node distances then describe two DIFFERENT coils. This does not crash -- it silently returns a plausible number, exactly the failure mode U6 exists to prevent. gate_u5u6.py:185 hardcodes the same pre-U4 formula as its reference, so the gate PASSES while the code is wrong -- the gate cannot catch this.

**FIX:** Delete the local reimplementation. In coil_topology.py, import and delegate: `from train_utils import _coil_bond_length` and have coil_bond_length(ca_folded) call it so the CFG.coil_b branch is honored -- or, if the import direction is unacceptable, replicate BOTH branches verbatim including the module constant _COIL_B_FIXED = _COIL_B_FIXED_ANGSTROM * _COIL_COORD_SCALE (5.82 * 0.1 = 0.582, train_utils.py:406-408) and the `raise ValueError("CFG.coil_b must be one of fitted|fixed")` guard. Note coil_ca_coords also currently knows nothing about the 10x NANO_TO_ANGSTROM coordinate rescale. Then change gate_u5u6.py step [7] to compare against train_utils._coil_bond_length under BOTH coil_b settings rather than against a hardcoded copy of the fitted formula, so the gate can actually detect divergence.

### [fatal] A FOURTH model call site exists, its batch is INTERLEAVED, and it already passes ca_coords -- so both the n_folded contract and the 'A6 is latent' premise are false. The worker flagged this as an open question for the manager but did not resolve it; it resolves AGAINST the patch. repo/train.py:342 and :418 call `E = model(X, ca_coords=ca_coords)` where `X = torch.cat((Xjf,Xju,Xd,Xcd,Xdu,Xcy1,Xcy2,Xcy3,Xcy4), dim=0)` (line 336) through the SAME PEM class (repo/train.py:4 `from model.hydro_net import PEM`). Row map: 0=Xjf folded, 1=Xju UNFOLDED, 2=Xd decoy, 3=Xcd decoy, 4=Xdu UNFOLDED, 5-8 cycles. The unfolded rows are 1 and 4 -- NOT a trailing contiguous block -- so no scalar n_folded can describe this layout, and apply_coil_edges/per_half_ca_coords would apply the coil to the wrong rows with no exception. Additionally, ca_coords IS passed here (built at repo/train.py:281 as cat([ca_n,ca_n,ca_n,ca_d,ca_n,ca_n,ca_n,ca_n,ca_n])) and at repo/train.py:651 (`model(X_pair, ca_coords=ca_single.expand(2,-1,-1))`, a folded/unfolded pair sharing one coordinate set). CFG.gat_cutoff = 12.0 (model/model_cfg.py:64), not None, so the CA-cutoff branch at hydro_net.py:593 is LIVE on that path. Therefore audit A6 is ACTIVE TODAY on the pretraining trainer, not latent -- PATCH.md's 'READ THIS SECOND -- A6 is LATENT' and coil_topology.py's 'ca_coords ... is passed by NOBODY' are true only of the Megascale slice in srclive/, not of the repo.

**FIX:** Replace the scalar boundary with an explicit row selector: change apply_coil_edges/per_half_ca_coords/split_folded_rows to accept `unfolded_rows` (a LongTensor or list of row indices) instead of n_folded, and have the two Megascale call sites pass `torch.arange(n, B)`. Keep a scalar n_folded convenience wrapper only if it raises when the layout cannot be expressed. As a minimum interim guard, make split_folded_rows reject any call it cannot describe rather than silently assuming a leading block. Separately, decide explicitly whether repo/train.py (which passes ca_coords through a live gat_cutoff=12.0) is in scope for U6; if it is not, U6 must hard-raise when it sees a batch from that path rather than half-applying. Update PATCH.md and coil_topology.py's module docstring, which both assert ca_coords is passed by nobody.

### [major] The import in Edit 4 will not resolve as written. PATCH.md says coil_topology.py is a 'New standalone file to drop next to train.py / hydro_net.py' and adds `import coil_topology as _coil` at the top of hydro_net.py. But those are two different directories in the real repo: train.py and train_utils.py are at repo root, while hydro_net.py lives in the model/ package (repo/model/hydro_net.py, with model/__init__.py present, and it imports siblings as `from model.model_cfg import CFG` -- an absolute package path, never a bare sibling import). A bare `import coil_topology` executed from inside model/hydro_net.py resolves only if repo root happens to be on sys.path at import time; train.py does `sys.path.append('./')` (train.py:6) which makes it work when launched from repo root, but it silently breaks for any other entry point or working directory. Edit 2 also does `from coil_topology import validate_coil_edges_flags` inside train.py, which resolves from root -- so the two edits assume two different resolution contexts for the same module.

**FIX:** Pick one location and use one consistent absolute import. Recommended: place the file at repo/model/coil_topology.py and import it as `from model import coil_topology as _coil` in model/hydro_net.py and `from model.coil_topology import validate_coil_edges_flags` in train.py -- matching the existing `from model.model_cfg import CFG` / `from model.hydro_net import PEM` convention. Update PATCH.md Edit 2 and Edit 4 accordingly, and fix gate_u5u6.py's `import coil_topology as CT` (line 27) to the same path so the gate exercises the real import.

**BLOCKER:** No torch in this environment (ModuleNotFoundError for both `python` and `py`), so gate_u5u6.py HAS NEVER BEEN EXECUTED. I verified instead: both .py files parse via ast; every PATCH.md anchor is byte-exact and unique against srclive (grep -F -c plus exact multi-line substring counts); and the full index arithmetic replayed in pure Python (folded 440 / coil 80 vs fully-connected 440 at N=11 n_folded=4 B=8; all |i-j|==1; no row crossing; symmetric; N=1 -> 0 edges; N=2 -> 2 edges; asymmetric split n_folded=3 -> 100 edges; monotone coil distances). The manager MUST run the gate first — seconds, no GPU, no data.

**BLOCKER:** OPEN QUESTION THE MANAGER MUST CLOSE BEFORE LAUNCHING --coil_edges: whether a FOURTH model call site exists outside these three files. train_utils.py builds cycle/decoy graphs (Xcy1, Xcy2, Xd, Xcd, Xdu) whose consumer I could not find in the srclive slice. If one of those is a folded+unfolded concat and does not get n_folded, U5 will silently apply in some places and not others — worse than not applying at all.

**BLOCKER:** Phase 7's ca_coords shape is unknown (nothing passes it today). per_half_ca_coords accepts [B,N,3], [n_folded,N,3] and [N,3] and raises otherwise; the manager should confirm which Phase 7 produces and delete the dead branches.

**BLOCKER:** Worth one grep over the launch scripts: if --gat_cutoff is set on any queued run, someone may already believe the GAT is distance-limited when it is in fact fully connected.

## u3u4  -- ready-with-fixes (fatal=0)

### [major] PATCH.md EDIT 3c ships a guard that would break EVERY run. Its third guard is `if _a.coil_offsets_path is not None and _a.coil_channels != 'offset': raise ValueError(...)`, written on the assumption `--coil_offsets_path` has `default=None`. The applied srclive (train.py:81) declares it `default=''`. I executed both spellings against real argparse: with `default=''`, `_a.coil_offsets_path is not None` is True on the plain default command line, so the guard raises ValueError on every invocation including `python train.py` with no coil flags at all. This is the ONLY remaining unapplied edit, so whoever applies it is reading PATCH.md, and PATCH.md still contains the poisoned form.

**FIX:** Apply PATCH_FOLLOWUP_guards.md, NOT PATCH.md EDIT 3c. The follow-up already uses the correct truthiness form `if _a.coil_offsets_path and _a.coil_channels != 'offset':`, which I verified is correct for both the `default=''` and `default=None` spellings across all 7 launch-case permutations (plain default proceeds; offset/fixed without --flory_unfolded raise; all four real cells proceed). Additionally, edit PATCH.md EDIT 3c in place to replace `_a.coil_offsets_path is not None` with `_a.coil_offsets_path` so the stale trap cannot be applied by a later reader.

### [major] `coil_offsets_path` is missing from the wandb run config, so the `offset` arm's actual numbers are unrecorded. train.py pushes it onto CFG (line 107, `CFG.coil_offsets_path = _a.coil_offsets_path or None`) and it demonstrably changes the graph, but the run-config dict at train.py:190-193 logs only `flory_unfolded`, `flory_nu`, `coil_channels`, `coil_b` — `coil_offsets_path` is absent. PATCH.md EDIT 3d specified all three keys; only two landed. I verified the consequence directly: with `coil_channels='offset'`, the built-in fallback table and a loaded offsets JSON produce non-equal unfolded graphs, yet both wandb rows read identically as `coil_channels: offset`. REPORT.md section 7 itself warns 'the fallback and the measured table are different numbers and the cells are not comparable across that choice' — this omission makes that distinction unrecoverable from the logs and dependent on a human remembering to write it down.

**FIX:** Add one line to the run-config dict in train.py immediately after `'coil_b': _a.coil_b,` (line 193, which occurs exactly once): `    'coil_offsets_path': _a.coil_offsets_path,`. Fold this into PATCH_FOLLOWUP_guards.md so it lands in the same pass as the guards.

**BLOCKER:** NOT BLOCKING, BUT APPLY BEFORE LAUNCHING: PATCH_FOLLOWUP_guards.md. The three validation guards are missing from the applied srclive. Verified failure mode: --coil_channels offset --coil_b fixed WITHOUT --flory_unfolded is torch.equal to the plain baseline while wandb logs the arm names. All four cells share that flag, so one omission in a launch script silently yields four baselines — indistinguishable from the expected null result. Until it is applied, check flory_unfolded==true on every U3/U4 wandb row before interpreting anything.

**BLOCKER:** Could not determine whether mask_tensor.pt is per-residue [N]; measure_coil_offsets.py falls back to all-ones on any shape mismatch and reports skips. Confirm on first run.

**BLOCKER:** Could not confirm whether coords_tensor.pt on disk is Angstrom or nm — NANO_TO_ANGSTROM's name and its effect (x0.1) point opposite ways, so the naming is untrustworthy. This does NOT affect my patch's correctness (I derived the scale self-consistently from fitted b on post-normalize_batch coords), but measure_coil_offsets.py --coord_scale should be sanity-checked on first run: its printed CA-CA neighbour distance must be ~3.8 in the Angstrom column.

**BLOCKER:** U5 (--coil_edges, A5) and U6 (per-half ca_coords, A6) also touch the coil. My edits are confined to _flory_unfolded_graph's node-feature path so they should not collide, but if U5/U6 land later the anchors must be re-checked — apply_patch_u3u4.py asserts single-occurrence rather than blind-replacing, and is idempotent (reports 'already applied' against current srclive).

**BLOCKER:** Did not measure the REAL per-protein spread of fitted b. The U4 arms only differ to the extent it varies across proteins; if real fitted b is tightly clustered near 0.38, cells 1/2 and 3/4 differ by almost nothing and U4 is answered trivially. Worth a two-minute check on real data before spending two of the four runs on it.

## w7edge  -- not-ready (fatal=2)

### [fatal] UNIT ERROR — the RBF bank is calibrated in Angstrom but the coordinates it receives are 10x smaller. train.py:219 normalize_batch does batch['coords'] = batch['coords'] * NANO_TO_ANGSTROM (NANO_TO_ANGSTROM = 0.1, train.py:35) BEFORE get_deltaG/get_wt_deltaG ever run, so the tensor T3 hands to stack_half_coords is in model units, not Angstrom. Real folded CA-CA neighbour distance is ~0.38, not 3.8. edge_features.rbf_expand uses linspace(0.0, 20.0, 16) with width=(hi-lo)/n=1.25, and --edge_rbf_hi defaults to 20.0 with help text 'Angstroms'. Simulated on a realistic 60-residue backbone scaled exactly as normalize_batch does: 7 of the 16 folded-half RBF channels have std < 1e-6 (identically dead), and every live response is crowded into the first few bins. The distance channel therefore carries far less information than the 16-dim width advertises, and it does so silently — the model trains and returns a plausible number. The tree ALREADY has the fix pattern and an explicit warning about this exact trap: train_utils.py:392-408 (_COIL_B_FIXED_ANGSTROM = 5.82, _COIL_COORD_SCALE = 0.1, _COIL_B_FIXED = 0.582) with a 15-line comment saying a literal Angstrom constant would make the arm '15x too large' and 'saturate the Gaussian kernel to ~0 everywhere'. This patch ignores that convention entirely.

**FIX:** Adopt train_utils._COIL_COORD_SCALE. In edge_features.py set RBF_HI_DEFAULT = 20.0 * 0.1 = 2.0 (model units) and document it in Angstrom-times-scale form exactly as _COIL_B_FIXED is; in PATCH.md T1 change --edge_rbf_hi default from 20.0 to 2.0 and change the help text from 'Angstroms' to 'model coordinate units (= Angstrom * NANO_TO_ANGSTROM)'. Equivalently, and preferably because it keeps the flag human-readable in Angstrom, convert inside build_edge_attr: after computing dist = delta.norm(dim=-1), divide by _COIL_COORD_SCALE (i.e. dist_A = dist / 0.1) before calling rbf_expand, and leave rbf_hi at 20.0. Pick one and state it in the docstring. Then add a gate assertion that pins the two together the way scripts/gate_u3u4.py test 8b pins fixed-b to fitted-b: build a folded CA trace with 0.38 model-unit neighbour spacing and assert that fewer than 2 of the 16 channels have std < 1e-6 across its edges.

### [fatal] UNFOLDED REFERENCE IS ~39x TOO LARGE — 81% of unfolded edges get an all-zero RBF vector, which hands the model a free folded-vs-unfolded indicator. edge_features.DEFAULT_BOND_LENGTH = 3.8 is an Angstrom constant, and unfolded_reference_coords uses it unconverted (pos = b * idx, or b * idx^nu) to build coordinates that are then compared against FOLDED coordinates already scaled to model units by normalize_batch. Measured on the same 60-residue simulation: folded max pair distance 5.80 model units, unfolded max pair distance 224.20, ratio 38.6x. Feeding both halves through build_edge_attr, 80.8% of unfolded-half edges have their ENTIRE 16-dim RBF vector below 1e-6 — exp(-((38.0 - c)^2)/(2*1.25^2)) underflows for every centre c in [0,20]. The RBF channel therefore does not encode unfolded geometry at all; it encodes a near-binary 'is this the unfolded half' flag. That is a trivially separable shortcut between the two states, which is precisely the leak U6 and the coil lever exist to remove, so this defeats the stated purpose of stack_half_coords while passing every assertion the gate makes (the gate only checks the two halves DIFFER, never that both land in the kernel's dynamic range). Compounding it, stack_half_coords never reads CFG.coil_b, so the 'fixed' arm silently diverges from train_utils._coil_bond_length (train_utils.py:481-499), contradicting the module's own claim to keep the edge coil and node coil 'the same curve'.

**FIX:** Two changes. (1) Scale the constant: set DEFAULT_BOND_LENGTH = 3.8 * 0.1 = 0.38 in model units, mirroring _COIL_B_FIXED's Angstrom-constant-times-_COIL_COORD_SCALE form, and update the gate assertion 'unfolded reference reads NO folded coordinate (b defaults to 3.8 A)' from abs(d01 - 3.8) < 1e-4 to abs(d01 - 0.38) < 1e-5. (2) Route b through the existing helper rather than reimplementing it: in stack_half_coords, replace the 'if b is None and flory and folded_coords.dim() == 3: b = float(mean_ca_bond_length(folded_coords))' branch with a call to train_utils._coil_bond_length(ca, N, ca.device, ca.dtype) so the fitted/fixed arms match the node path exactly, and delete mean_ca_bond_length (which duplicates _coil_bond_length's 'fitted' branch while ignoring CFG.coil_b). If keeping edge_features.py import-free of the repo is required for the standalone gate, pass b down from the T3 call site in train.py instead, where train_utils is already imported. Then add a gate check asserting max unfolded pair distance is within ~2x of max folded pair distance for a realistic backbone.

### [major] THE EDGE PATH IGNORES batch['masks'] — padded/masked residues contribute real edge attributes. Every node path in the tree zeroes masked residues: get_graph does mask_index = torch.where(mask == 0); D[mask_index[0],:,:] = 0; D[:,mask_index[0],:] = 0 (train_utils.py:311-313), and _flory_unfolded_graph repeats it verbatim (train_utils.py:637-639). build_edge_attr takes no mask argument, and neither T3's stack_half_coords call nor H3's build_edge_attr call passes batch['masks'], which is already on the device at both call sites. Masked residues are therefore given a genuine one-hot pair and a genuine CA-CA distance in the edge channel and participate normally in GATv2 attention. This is a real inconsistency with the node features rather than a crash, so it will not surface in any gate as written.

**FIX:** Add a mask parameter to build_edge_attr: build_edge_attr(edge_index, one_hot_flat, ca_coords, N, mask=None, ...). When mask is not None (shape [B,N] or [N] broadcast over B), compute keep = mask_flat[src].bool() & mask_flat[dst].bool() and zero the full attribute row for dropped edges (attr[~keep] = 0), matching the node path's zeroing convention rather than deleting edges (deleting would change edge_index and desynchronise it from the tensor the GAT layers already received). Thread it through: in PATCH.md H3, PEM.forward has no mask, so pass it in as PEM.forward(self, x, f_type='Default', ca_coords=None, node_mask=None) and supply batch['masks'].squeeze() from T3's two call sites alongside ca_coords. Add a gate check that a masked residue's incident edges are all-zero.

### [major] T3 UNCONDITIONALLY DISABLES THE EDGE-INDEX CACHE AND SILENTLY ACTIVATES THE DEAD --gat_cutoff PATH. get_edge_index caches only when ca_coords is None (hydro_net.py:556-559 guard the read, :606-608 the write). Today ca_coords is never passed anywhere in srclive (I confirmed: grep for ca_coords across train.py and train_utils.py returns no matches), so the cache always hits. With --edge_features on, T3 passes a non-None ca_coords on every get_deltaG and get_wt_deltaG call, so the cache is dead for the whole run and the O(N^2) meshgrid is rebuilt every minibatch of every epoch. Separately and more importantly for interpretation: because ca_coords is now non-None, the branch 'if ca_coords is not None and self.gat_cutoff is not None' (hydro_net.py:593) becomes reachable for the first time, so any run that also sets CFG.gat_cutoff silently switches from a fully-connected GAT to a distance-cutoff GAT. That changes the edge SET, not just the edge ATTRIBUTES, so --edge_features would no longer be the clean attribute-only ablation it is named and documented to be — it would confound the attribute lever with the edge-set lever that --gcn_span/--gcn_bidir and gate_w7.py exist to isolate. The report acknowledges 'T3 makes --gat_cutoff live for the first time' as an open question but ships no guard for it.

**FIX:** Two changes. (1) Preserve the ablation: in H4, alongside the existing cutoff-vs-rbf_hi check, raise when both are set unless the manager opts in explicitly — if self.gat_cutoff is not None: raise ValueError('[W7] --edge_features with --gat_cutoff changes the edge SET as well as the edge ATTRIBUTES; ca_coords was never passed before this patch, so --gat_cutoff was inert and is now live. Run --edge_features at gat_cutoff=None for the attribute-only ablation, or add --allow_cutoff_with_edge_features to confound them deliberately.') Add that opt-in flag in T1/T2 if the manager wants the combination. (2) Restore caching: extend the cache key from (B, N) to (B, N, id(ca_coords)) is not safe across steps, so instead cache the fully-connected GAT index separately from the cutoff index — keep the existing (B,N) cache and consult it whenever self.gat_cutoff is None, regardless of whether ca_coords was supplied, since in that case the GAT edge set genuinely does depend only on shape.

**BLOCKER:** U6 IS A HARD DEPENDENCY AND IS OWNED BY ANOTHER WORKER. ca_coords is never passed anywhere in srclive, so PATCH.md edit T3 is what supplies it — per half. That IS the U6 fix for the edge path only. If U6's owner lands different ca_coords plumbing in get_deltaG, T3 must be RECONCILED with theirs, not applied alongside: two sources of ca_coords in get_deltaG is a merge conflict, not a merge.

**BLOCKER:** --edge_features x --loss_mode ddg_head is currently UNGUARDED. get_ddg_head does not pass ca_coords (deliberately — it is folded-only, so no U6 hazard), which means every edge distance would be 0 and the RBF channel a constant column. My H4 guard is written getattr(CFG,'loss_mode','dg') and is INERT because CFG.loss_mode does not exist — train.py keeps it in module-level LOSS_MODE. To arm it the manager must add CFG.loss_mode = _a.loss_mode next to the T2 block. I chose inert over assumed so applying H4 alone cannot break the ddg_head arm.

**BLOCKER:** G1 byte-identity could not be run here — no torch locally (ModuleNotFoundError). gate_w7_edge.py prints SKIP for its in-tree section; a SKIP IS NOT A PASS and --edge_features must not train until G1 has run in-tree with torch.equal (never allclose) against the pre-patch build. The REPORT gives the exact git-stash procedure.

**BLOCKER:** The installed torch_geometric version could not be determined, and the edge_dim/edge_attr keyword assumption rests on it. Verify at apply time.

**BLOCKER:** gate_w7.py must be re-run after this patch and must still be 11/11 — H1-H3 change forward_gat's signature and the GAT layer constructor, which that gate exercises.

## w9  -- ready-with-fixes (fatal=0)

### [major] desc_dim_of() reads CFG.aa_desc_dim, which is NEVER SET ANYWHERE in the live tree. Verified: `grep -rn aa_desc_dim train.py train_utils.py hydro_net.py aa_descriptors.py` returns only hydro_net's own local function _aa_desc_dim and its call site — train.py L118-119 sets CFG.aa_descriptors and CFG.aa_descriptor_csv but never a width attribute. hydro_net gets the width from aa_descriptors.descriptor_dim(mode,cfg), which READS THE CSV, not from CFG. So metal_features.metal_start(cfg) raises ValueError for any cfg with aa_descriptors!='none'. Confirmed by execution. The worker flagged this as an inference (A9) but it is a confirmed mismatch, not an open question. Production impact is nil today because metal_start() is never called on the training path (hydro_net computes self.metal_start = self.desc_start + self.desc_dim independently, which is correct) — but it is a live landmine for any caller and it makes gate G3 a false reassurance.

**FIX:** Replace the body of desc_dim_of in metal_features.py to use the same authoritative source hydro_net uses, falling back to CFG only if the module is absent:

    mode = getattr(cfg, 'aa_descriptors', 'none') or 'none'
    if mode == 'none':
        return 0
    try:
        from aa_descriptors import descriptor_dim
        return int(descriptor_dim(mode, cfg))
    except ImportError:
        k = getattr(cfg, 'aa_desc_dim', None)
        if k is None:
            raise ValueError(...existing message...)
        return int(k)

Then drop the PATCH.md EDIT 2 sentence 'reading CFG.aa_desc_dim (which W6's wiring must publish)' — no such wiring exists or is needed.

### [major] PATCH.md EDIT 2 claims "gate_w9.py G3 asserts the two agree across all four W5 x W6 combinations", i.e. that the gate cross-checks metal_features.metal_start() against hydro_net's self.desc_start + self.desc_dim. It does not. gate_w9.py never imports hydro_net, never constructs a PEM, and never calls _aa_desc_dim or descriptor_dim (grep for hydro_net|PEM|_aa_desc_dim|descriptor_dim in gate_w9.py returns only a comment on L250). G3 compares metal_start(cfg) against its own locally recomputed `48 + solv + desc_k`, using a _Cfg stub whose aa_desc_dim it sets by hand — so it re-asserts the helper's arithmetic against itself and would pass even if hydro_net's offset disagreed. The one cross-implementation agreement the patch most depends on is unverified.

**FIX:** Either (a) soften the PATCH.md claim to 'G3 checks the helper's arithmetic; the hydro_net offset must be confirmed separately on the cluster', or (b) better, add a real cross-check to gate_g3 guarded by an import so it skips cleanly off-cluster:

    try:
        from hydro_net import _aa_desc_mode, _aa_desc_dim
        hn_desc = _aa_desc_dim(_aa_desc_mode(cfg), cfg)
        check('G3 hydro_net desc_dim agrees with metal_features (%s)' % tag,
              48 + solv + hn_desc == metal_start(cfg))
    except ImportError:
        print('    SKIP G3 hydro_net cross-check: training package not importable')

**BLOCKER:** HARD EXTERNAL DEPENDENCY: metal_sites.csv does not exist. ANNOTATION_SPEC.md is written to be sent as-is with a date blank at the top. Until it arrives the arm cannot run and gate G4 cannot execute. --metal_features RAISES if set without --metal_csv, by design: an all-zero block would burn a run and report as a null result indistinguishable from a real one.

**BLOCKER:** The preprocessed tensors carry NOTHING usable — no HETATM, no ligand, no B-factor, no PDB/CIF. Metal identity and geometry are unrecoverable from the data as stored. This is missing information, not missing code.

**BLOCKER:** OPEN QUESTION worth asking BEFORE sending the spec: do raw PDB/CIF files exist anywhere outside this tree? If the group kept the structures used to build the tensors, the annotation could be derived in-house from HETATM records and the external dependency disappears entirely. The audit script searches the data roots but cannot search the whole filesystem.

**BLOCKER:** The graph-node form is blocked by more than annotations: N is fixed by coords/masks; adding a node breaks ca_coords, the mask, and the 20-wide right-anchored one_hot; _dg_norm's divisor would shift dG for metalloproteins for reasons unrelated to metal chemistry; the energy sum and the folded/unfolded stack-and-split-in-half require equal node counts in both passes; and get_edge_index has no edge-type channel. REPORT.md section 5 specifies a cheaper virtual-node intermediate that keeps N fixed, recommended ONLY if the annotation returns real sites.

**BLOCKER:** I could not determine the exact residue-index convention used when the tensors were built (SEQRES position 1 vs first observed residue vs other). This is why the spec defines the convention by reference to the stored tensor and why G4 hunts the offset in [-5,+5] rather than trusting memory.

**BLOCKER:** I could not read W6's train.py CFG wiring, so the attribute name CFG.aa_desc_dim is inferred from its hydro_net helpers. desc_dim_of raises rather than guessing, so a mismatch fails loudly at startup rather than silently at run time.

**BLOCKER:** Gates were verified against a numpy torch shim — the authoring machine has no torch. The assertions are pure shape/value arithmetic so they carry over, but gate_w9.py must be re-run once on the cluster under real torch before it is trusted. The shim (_shim/torch.py) is NOT a deliverable and must be left out of the cluster copy.

**BLOCKER:** G1 as run is a simulation of the torch.cat arithmetic; it cannot import train_utils without the training package. After applying the patch, the real G1 (torch.equal on Fh from the actual get_graph, flag off, vs a pre-patch tensor, for both unfolded builders with and without --flory_unfolded) still has to be run.

**BLOCKER:** PATCH.md EDIT 10 is the one edit whose anchor depends on the training loop. The RECOMMENDED form is a single insert inside normalize_batch (which every path goes through) rather than patching six call sites, because a missed site silently reuses the previous protein's annotation — wrong rather than absent, and nothing else in the system would notice.

## monitor  -- not-ready (fatal=2)

### [fatal] A2 (submit_factorial.sh) inlines ${TRAIN_CMD} unquoted after the `--` separator, but TRAIN_CMD (submit_factorial.sh:64-70) contains embedded semicolons: 'module load anaconda; source activate esm2_env_py38; export WANDB_MODE=...; export PYTORCH_CUDA_ALLOC_CONF=...; python Megascale-fineTuning/train.py ...'. The compute node's shell splits on those semicolons. I captured the real wrap string with a fake sbatch and executed it: run_supervised.sh receives only 6 args (arg1=TAG, arg2=logs/TAG_999.out, arg3=--, arg4=module, arg5=load, arg6=anaconda). The supervisor therefore supervises `module load anaconda`, exits immediately, and `python train.py` runs as a SEPARATE statement OUTSIDE the supervisor - completely unmonitored. Worse, TRC=$? captures the exit status of `source activate`, not training, so a crashed training run reports success and the --dependency afterok eval fires on a broken checkpoint. No monitor stop rule (M4/M5) is ever armed. This does not crash; it silently produces a plausible-looking run with no supervision, which is precisely the failure mode the whole item exists to prevent. B2 (sidearms) does NOT have this bug - it correctly hoists a semicolon-free SA_TRAIN - so the two halves of the patch disagree.

**FIX:** Mirror B2 in A2. In submit_factorial.sh, split TRAIN_CMD into an environment prefix and a semicolon-free command: keep 'module load anaconda; source activate esm2_env_py38; export WANDB_MODE=disabled; export PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True;' in the wrap prefix, and define FAC_TRAIN="python Megascale-fineTuning/train.py --full_data --no_pretrain --no_freeze --loss_mode ddg --pooled_corr_weight 0 --dg_length_norm none --affine_calib --wt_anchor_weight ${AW} --designed_weight ${BW} --slope_weight ${CW} ${DD} --val_frac 0.1 --epochs 15 --seed ${SEED} --run_tag ${RUN_TAG}" with NO semicolons. Pass only ${FAC_TRAIN} after the `--`. Keep the existing TRAIN_CMD definition for the SUPERVISE=0 branch so inertness is preserved. ALSO delete the PATCH.md paragraph 'Note on ${TRAIN_CMD} inside WRAPPED ... Leave both.' - it explicitly instructs the manager to preserve the broken construction.

### [fatal] The supervisor's TRAIN_LOG is the SAME FILE as SLURM's job output. The wrap sets SUPLOG=logs/${RUN_TAG}_\$SLURM_JOB_ID.out and sbatch is invoked with --output "logs/${RUN_TAG}_%j.out" --error same - these resolve to an identical path. run_supervised.sh:79 then does `: > "$TRAIN_LOG"` (truncate) and line 85 does `"$@" 2>&1 | tee -a "$TRAIN_LOG"` while SLURM still holds that same file open on its own descriptor. I modeled this with a held-open fd plus an external truncation: the resulting file is '^@^@^@...^@TRAIN LINE 1 / TRAIN LINE 2 / TRAIN LINE 2' - NUL-byte padding from SLURM's retained write offset, plus every training line written twice (once by tee, once by SLURM's inherited stdout). Consequences: monitor.py counts each epoch twice so its per-epoch jsonl and stop rules operate on doubled data; epoch2_check.sh's rows[2] is no longer epoch 2; and run_calib_eval.sh's best-epoch index (which the eval job depends on) shifts, selecting the wrong checkpoint. Silent wrong numbers, no crash.

**FIX:** Give the supervisor a PRIVATE log distinct from SLURM's. In both A2 and B2 change the wrap to SUPLOG=logs/${RUN_TAG}_\$SLURM_JOB_ID.suplog (any extension SLURM does not own). In run_supervised.sh delete line 79 (`: > "$TRAIN_LOG"`) entirely - do not truncate a file another process may hold open - and keep only the `tee -a`. Leave the sbatch --output/--error paths and the TRAIN_LOG= line that feeds run_calib_eval.sh exactly as they are, so the eval dependency still resolves to SLURM's own clean log.

### [major] autodebug.py:367 selects a GPU by partition name: parts = ["--partition=gpu-a", "--partition=gpu-b"], used at :369-370 for the PENDING>12h rule. This directly violates the site rule recorded in submit_factorial.sh:29 ('--gres=gpu:rtx_6000:1  # NEVER by partition name: submit filter reroutes onto a 1080 -> OOM'). autodebug.py's own docstring at :433-435 states the prohibition and then the code breaks it 70 lines earlier. The partition names are additionally invented placeholders - the worker admits this - so any emitted resubmit command names partitions that do not exist AND drops the --gres spec that keeps the job off a 1080.

**FIX:** Delete the partition branch. Replace the PENDING_LONG action body with either (a) act.update(record_only=True, note='PENDING>12h: operator decision; real partition names must come from CFG, and --gres=gpu:rtx_6000:1 must be preserved'), or (b) a --exclude/--constraint based action that keeps GPU_SPEC intact and never emits --partition. Remove the corresponding selftest expectation. Do not ship any --partition string until real names are read from CFG.

### [major] autodebug.py emits resubmit commands using two train.py flags that DO NOT EXIST. I enumerated every add_argument in srclive/train.py: the complete flag set is aa_descriptor_csv, aa_descriptors, affine_calib, burial_features, burial_mode, coil_b, coil_channels, coil_offsets_path, ddg_weight, designed_weight, dg_length_norm, epochs, flory_nu, flory_unfolded, fold, full_data, gcn_bidir, gcn_span, loss_mode, max_folds, no_freeze, no_pretrain, pooled_cap, pooled_corr_weight, pooled_window, readout, run_tag, seed, slope_weight, unfolded_emb, unfreeze_epochs, unfreeze_from, unfreeze_lr, val_frac, wt_anchor_weight. Neither --mini_batch (autodebug.py:357, OOM attempt 3) nor --ranking_weight (autodebug.py:402, NaN retry) is present. MINI_BATCH_SIZE is a hardcoded module constant at train.py:41 (=64) with no CLI wiring at all. Both retry paths therefore emit sbatch/train commands that argparse rejects with 'unrecognized arguments', so the retry dies within seconds and the cell is lost. autodebug.py --selftest passes because it only asserts on its own action dict, never against train.py's real parser.

**FIX:** For --mini_batch: either add `_p.add_argument('--mini_batch', type=int, default=64)` to train.py and set MINI_BATCH_SIZE = _a.mini_batch, or change the OOM attempt-3 branch to record_only with a note that batch reduction requires a code change. For --ranking_weight: replace it with a flag that actually exists and matches the intended NaN mitigation - --slope_weight 0 or --pooled_corr_weight 0 are the real loss-term switches - and update the corresponding selftest assertion at autodebug.py:789. Additionally, add a startup check in autodebug.py that any key in train_arg_override is grepped out of train.py's add_argument list, so this class of drift fails the selftest instead of a live run.

**BLOCKER:** Could not determine the REAL format of train.py's log lines — no cluster, no data, and I never ran train.py. monitor.py's own header admits it was tested 'in isolation only ... NOT exercised: live attachment to a running train.py'. The whole monitor/supervisor chain is therefore verified against SYNTHETIC logs matching the regexes in monitor.py and run_calib_eval.sh. The first supervised run is the real test: check results/<tag>_monitor.jsonl has one row per epoch with non-null ddg_loss and val_pp. If null, monitor.py's regexes need widening — a monitor.py change, not a supervisor change.

**BLOCKER:** Cannot verify tail -F and pkill -P behaviour on the compute nodes; tested on Windows/MSYS bash. Both are coreutils/procps standard but the compute-node shell is unverified.

**BLOCKER:** Real partition names for the PENDING>12h rule are unknown (see assumptions).

**BLOCKER:** cluster_run/scripts/resubmit_one.sh does not exist — autodebug emits commands referencing it. Someone must write it or treat the emitted line as human instructions.

**BLOCKER:** Could not find any existing results/state.json anywhere in scratchpad or repo, so its real schema (if one exists on the cluster) is unverified. load_state tolerates a missing file and preserves unknown keys; save_state writes atomically via os.replace so a concurrent submitter cannot tear the allowlist — but it does NOT lock. Wave submission is serial (waves of 8, verify before next) so this is acceptable; if that ever changes, add file locking.

**BLOCKER:** The '3 pilots' have no job ids recorded anywhere I could find. Under the current gate an unrecorded pilot is MAXIMALLY protected (unlisted => never cancellable), which is the correct failure direction.

**BLOCKER:** NO CLUSTER ACCESS as instructed — nothing was applied, submitted, cancelled or run against SLURM. The manager must apply PATCH.md and run the verification block in its section C before the next wave.

## e2e  -- ready-with-fixes (fatal=0)

### [major] check_1_flags scores the same 'environment unavailable' condition two contradictory ways. At verify_e2e.py:329 an import failure records c.sub(label, False, 'INCONCLUSIVE: import failed before parsing') — a hard FAIL sub-assertion — while at line 340 the equally-unknowable 'validator not reachable without the dataset' records c.sub(..., True). Because Check.status is `all(s[1] for s in self.subs)`, check 1 reports FAIL on any host lacking sklearn/torch/wandb, and the process exits 1, indistinguishable in CI from a genuine lever regression. I reproduced this: running against a root built from the live sources gave 'FAIL check 1' with six FAILED SUBs all reading INCONCLUSIVE. This directly violates the suite's own documented principle at lines 274-277 ('If we accepted rc != 0 as rejection ... this check would pass VACUOUSLY') — the author guarded against vacuous PASS but created a vacuous FAIL in the other direction, which will train operators to ignore check 1.

**FIX:** At verify_e2e.py:329 change `c.sub(label, False, 'INCONCLUSIVE: import failed before parsing (rc=%d)' % rc2)` to `c.sub(label, True, 'INCONCLUSIVE: import failed before parsing (rc=%d) -- see SKIP note' % rc2)`, matching the treatment already used at line 340. The loudness is preserved by the existing `if unknown:` branch at line 387, which already emits a c.skip() note naming the count and the needed environment. Do not instead flip line 340 to False — that would make the suite unrunnable off-cluster entirely.

### [major] CONFIRMED (the worker reported this as an unverified suspicion; it is real). AUTOPILOT.md D1 states 'a cell that raises pooled while collapsing slope below 0.3 median is not selected', but autopilot.py:322 selects with `best = max(rows, key=lambda r: r['pooled'])` and no slope predicate anywhere in one_pass. I grepped the whole file: slope_med and slope_min are parsed by calib_diag at lines 160-161 and written to the TSV at lines 249-250, but are never read in any comparison or filter. The driver can therefore select exactly the cell D1 forbids — the anchor's known failure mode, where a lever inflates pooled ddG while flattening the per-protein slope. This is a silent-wrong-answer defect: it produces a plausible winning tag and writes it into the thesis, with no crash and no warning.

**FIX:** In autopilot.py one_pass, filter before taking the max, and log when the filter bites so the exclusion is on the record: replace `best = max(rows, key=lambda r: r['pooled'])` (line 322) with `ok = [r for r in rows if r.get('slope_med') is not None and r['slope_med'] >= 0.3]`, then `if len(ok) < len(rows): log('D1: excluded %d cell(s) with slope_med < 0.3: %s' % (len(rows)-len(ok), ', '.join(sorted(r['tag'] for r in rows if r not in ok))))`, then `best = max(ok or rows, key=lambda r: r['pooled'])`. The `or rows` keeps the pass from crashing when every cell collapses, but that case should also halt — pair it with a halt() if `not ok`. Hardcode nothing else: 0.3 is the documented D1 constant and belongs beside EFFECT_MIN at line 33 as SLOPE_MIN, not inline.

**BLOCKER:** NEEDS A HUMAN DECISION: E2E-1's fix direction. AUTOPILOT.md D1 defines the threshold but never the estimator. If the intended convention is regression half-coefficients for BOTH, then main_effects is wrong in the other direction and PATCH.md is backwards. Either way the two must agree, because one threshold judges both. I did not guess.

**BLOCKER:** SUSPECTED THIRD GAP, unverified, same severity as E2E-1: AUTOPILOT D1 says 'a cell that raises pooled while collapsing slope below 0.3 median is not selected'. I found NO implementation in autopilot.py — one_pass picks best = max(rows, key=pooled) with no slope condition. If that reading is right, the driver can select exactly the cell D1 forbids (the anchor's known failure mode). I wrote no check because I could not confirm it isn't enforced elsewhere. Someone should confirm.

**BLOCKER:** Gate scripts never executed against real code (they import model.model_cfg / model.hydro_net / train_utils, absent from my sandbox). Needs: python verify_e2e.py --root <repo> from the REPO ROOT on the login node in the training env (module load anaconda; source activate esm2_env_py38). CPU-only, no GPU. This is the highest-value thing to run and turns check 2 into a real PASS/FAIL.

**BLOCKER:** --flory_nu 1.7 rejection unproven: its module-scope raise sits before the data load, but my sandbox has no dataset so the process died on the missing directory first. Reported as INCONCLUSIVE, never as a pass. Needs a checkout where ./data/Processed_K50_dG_datasets/training_data exists.

**BLOCKER:** autopilot.calib_diag's regex parsing is UNTESTED by this suite and is the biggest remaining risk surface — it parses [ALL] vs [DESIGNED] blocks by regex, and a malformed parse returns None and silently drops the cell from D1. Same class of silent-wrong-answer as E2E-1. Needs real eval CSVs.

**BLOCKER:** No real SLURM interaction (deliberate — stub sbatch only). To confirm SLURM honours afterok, inspect an already-queued eval job read-only: scontrol show job <evalid>, check its Dependency. Safe with the 48 runs in flight.

**BLOCKER:** H1 and H3 tested as predicates I wrote, since neither exists in autopilot.one_pass. Needs a decision: implement H1 in the S0 script and H3 as a self_audit escalation, or accept both as operator-procedure halts.

## catalogue  -- ready-with-fixes (fatal=0)

### [major] catalogue_report.md — named in REPORT.md as "**the deliverable**" — is a STALE earlier draft that the audit superseded but never replaced, and it contradicts REPORT.md on the item's central verdict. Its section 4 is titled "W8 substitute: resolution DOES support a structure-level tercile analysis" and calls the result "clean", "real, reproducible, monotone", while REPORT.md and the verified driver output conclude "W8 stays blocked" and "monotone inside window: False" (line 101 of audit_out.txt). Section 4 also asserts "BSA has the largest standardized coefficient of the three" from a 3-predictor OLS, which the driver's 9-predictor OLS (including log10 per-chain length) does not support. Critically, the deliverable contains NEITHER finding the audit actually rests on: grep for per-chain / -0.4883 / -0.3262 / assembly length returns zero hits, and the in-window collapse (+0.047, non-monotone terciles) is absent. A reader sent to the named deliverable gets the pre-correction conclusions.

**FIX:** Regenerate catalogue_report.md from the verified audit_out.txt before this item is used. Concretely: (a) retitle section 4 to the actual verdict — resolution does NOT usefully substitute, citing T3-T1 = +0.209 sd catalogue-wide collapsing to +0.047 with non-monotone terciles (T2 -14.70 worse than T3 -15.17) inside the per-chain 30-80 aa window; (b) delete or rewrite the "BSA has the largest standardized coefficient" sentence, replacing the 3-predictor OLS with the driver's 9-predictor OLS (R^2=0.0685 -> the reported ~0.09 figure) that includes log10 per-chain length; (c) add the section-6 per-chain-length finding (corr(AA Length, n_chains)=+0.9716; log10 per-chain length -0.3262; monomers-only -0.4883; partial|resolution -0.2415; the 30-50 aa +1.82 sd / 50-80 aa +0.71 sd short-chain table) which is currently missing entirely.

**BLOCKER:** Could NOT verify the join to the ddG benchmark: no MegaScale/ddG file exists anywhere under C:\Users\User\Downloads\, and I have no cluster access. Every statement about relevance to the benchmark is by SIZE-WINDOW PROXY, not by an actual join. This is the single biggest gap — a worker holding the benchmark should join on protein_id (case-sensitively) and report the overlap count; if it is near zero, the short-chain transfer hypothesis becomes untestable from this file and only the size-regime argument survives.

**BLOCKER:** Could not determine why 26,962 ids are SCOP/ASTRAL domains (2CRV_d2crva1) rather than PDB entities. Two corpora appear to have been concatenated; their loss means differ (-18.52 vs -17.96) and median lengths differ (623 vs 436), so the mixture is not innocuous.

**BLOCKER:** Could not determine what the {10,20,30,40,50}# prefix on the 160 validation ids encodes — exactly 32 rows per bin, so a stratified sample over five strata, but of what is unknown. Those rows have no annotations joined at all, so the validation split is un-analysable in this file.

**BLOCKER:** Could not explain why 233 rows of the BSA column read the literal string 'Error', nor why 48 rows disagree between the two BSA columns by up to 43.9. Immaterial at n~100k but it is unexplained upstream breakage in the same tool that produced the corrupt columns.

**BLOCKER:** W8 remains BLOCKED. This catalogue does not unblock it: resolution is per-crystal not per-residue, and its effect vanishes (+0.047, non-monotone terciles) in the benchmark's size regime. Delivering PHASE 8's paragraph requires PDB metadata lookups for the MegaScale parent proteins, which is a separate item.